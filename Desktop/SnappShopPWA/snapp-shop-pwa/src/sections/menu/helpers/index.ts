import {CTX} from '@root/types'

import i18n from '@i18n'
import menuFa from '@assets/locales/menu/fa.json'

const menuSideEffects = (ctx: CTX) => {
  const {store} = ctx
}

i18n.addResourceBundle('fa', 'menu', menuFa)
export default menuSideEffects
